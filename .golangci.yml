version: 2
linters-settings:
  lll:
    line-length: 120
    tab-width: 4
linters:
  enable:
    - lll           # line length control
    - govet
    - revive
formatters:
  enable:
    - gofmt
    - gofumpt
